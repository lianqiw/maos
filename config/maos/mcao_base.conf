#File mcao_base.conf
#A basic MCAO system with no implementation errors.
__reset__         #replace all previous entries.
__default__	      #this file contains default setup.

#defines simulation parameters.
include     = "sim.conf"

#AO type. 
include     = "dm_dual.conf" #MCAO with dual DMs

#Atmospheric profile
include     = "atm_mk13n50p.conf" #Mauna Kea 13N 50%.

#Wavefront sensor information is included separately.

#Evaluation and fitting FoV information.
include     = "fov_sq34.conf"

#Including configurations for reconstruction (tomo/fitting)
include     = "recon.conf"

#For debugging purpose
include     = "dbg.conf"
